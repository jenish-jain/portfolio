{
  "name": "portfolio",
  "version": "0.1.0",
  "homepage": "http://jenish-jain.in",
  "dependencies": {
    "@testing-library/jest-dom": "^5.15.0",
    "@testing-library/react": "^11.2.7",
    "@testing-library/user-event": "^12.8.3",
    "dotenv": "^16.0.1",
    "gray-matter": "^4.0.3",
    "marked": "^17.0.0",
    "matter-attractors": "^0.1.6",
    "matter-js": "^0.12.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-ga": "^3.3.1",
    "react-helmet": "^6.1.0",
    "react-router-dom": "^5.3.4",
    "react-scripts": "4.0.3",
    "web-vitals": "^1.1.2"
  },
  "scripts": {
    "generate-resume": "node scripts/generateResumeContent.js",
    "prestart": "npm run generate-resume",
    "start": "react-scripts start --openssl-legacy-provider",
    "prebuild": "npm run generate-resume",
    "build": "react-scripts build --openssl-legacy-provider",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "clear-old": "rm -rf node_modules/.cache/gh-pages",
    "deploy": "gh-pages -b production -d build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^3.2.3"
  }
}
