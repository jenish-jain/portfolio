[build]
  base = "apps/blog"
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "22"

[dev]
  command = "npm run dev"
  targetPort = 8080
  framework = "#custom"
  functions = "netlify/functions"
