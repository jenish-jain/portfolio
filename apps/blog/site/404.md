---
layout: bento.njk
title: 404 — Page Not Found
seo_title: Page Not Found
description: This page does not exist.
permalink: 404.html
---

<main class="error-page">
  <div class="error-code">/404</div>
  <h1 class="error-title">Page not<br><em>found</em>.</h1>
  <p class="error-sub">This page doesn't exist — it may have moved or never existed at all.</p>
  <div class="error-actions">
    <a href="/" class="error-cta">← go home</a>
    <a href="/posts/" class="error-link">browse posts</a>
  </div>
  <p class="error-report">
    Got here from a link?
    <a class="reporting-link" href="https://github.com/jenish-jain/blog.jenishjain.in/issues/new">open an issue</a>
    or email <a href="mailto:jenishjain6@gmail.com">jenishjain6@gmail.com</a>
  </p>
</main>

<style>
  .error-page {
    max-width: 1100px;
    margin: 0 auto;
    padding: clamp(64px, 12vw, 140px) clamp(20px, 4vw, 48px) 80px;
  }
  .error-code {
    font-family: var(--mono);
    font-size: 11px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--muted);
    margin-bottom: 20px;
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .error-code::before {
    content: "";
    width: 6px;
    height: 6px;
    background: var(--accent-3);
    border-radius: 1px;
  }
  .error-title {
    font-family: var(--display);
    font-size: clamp(56px, 10vw, 128px);
    font-weight: 400;
    line-height: 0.95;
    letter-spacing: -0.02em;
    margin: 0 0 28px;
    color: var(--ink);
  }
  .error-title em { font-style: italic; color: var(--accent-3); }
  .error-sub {
    font-size: 18px;
    line-height: 1.6;
    color: var(--muted);
    max-width: 44ch;
    margin: 0 0 36px;
  }
  .error-actions {
    display: flex;
    gap: 16px;
    align-items: center;
    margin-bottom: 48px;
  }
  .error-cta {
    font-family: var(--mono);
    font-size: 12px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    padding: 12px 24px;
    background: var(--ink);
    color: var(--paper);
    border-radius: 999px;
    text-decoration: none;
    transition: background .2s;
  }
  .error-cta:hover { background: var(--accent); color: var(--ink); }
  .error-link {
    font-family: var(--mono);
    font-size: 11px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--muted);
    text-decoration: none;
    border-bottom: 1px solid var(--hair);
    padding-bottom: 2px;
    transition: color .18s, border-color .18s;
  }
  .error-link:hover { color: var(--ink); border-color: var(--ink); }
  .error-report {
    font-family: var(--mono);
    font-size: 11px;
    letter-spacing: 0.06em;
    color: var(--muted);
    margin: 0;
  }
  .error-report a { color: var(--muted); transition: color .15s; }
  .error-report a:hover { color: var(--ink); }
</style>

<script>
  const link = document.querySelector('.reporting-link');
  if (link) {
    const url = new URL(link.href);
    url.searchParams.set('title', 'Broken link: ' + window.location.href);
    link.href = url;
  }
</script>
